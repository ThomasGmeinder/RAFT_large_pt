{
  "name": "RAFT Large",
  "build": {
    "dockerfile": "Containerfile",
    "context": ".."
  },
  "workspaceFolder": "/workspaces/RAFT_large_pt",
  "remoteUser": "vscode",
  "postCreateCommand": "sudo chown vscode:vscode /home/vscode/.cache /home/vscode/.cache/torch && bash setup_venv.sh 10.0.0 && uv pip install --python .venv-rocm-10.0.0/bin/python --system-certs -r requirements.txt && .venv-rocm-10.0.0/bin/python -m ipykernel install --user --name raft-large --display-name \"RAFT Large (ROCm 10)\"",
  "runArgs": [
    "--device=/dev/kfd",
    "--device=/dev/dri",
    "--group-add=44",
    "--group-add=992",
    "--ipc=host",
    "--init"
  ],
  "mounts": [
    "source=raft-large-pt-torch-cache,target=/home/vscode/.cache/torch,type=volume"
  ],
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "ms-toolsai.jupyter",
        "ms-azuretools.vscode-docker"
      ],
      "settings": {
        "python.defaultInterpreterPath": "/workspaces/RAFT_large_pt/.venv/bin/python"
      }
    }
  }
}
